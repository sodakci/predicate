diff --git a/SER/ser-result-detector/src/main/java/verifier/SERSolverAR.java b/SER/ser-result-detector/src/main/java/verifier/SERSolverAR.java
index 37689f7..b46f98c 100644
--- a/SER/ser-result-detector/src/main/java/verifier/SERSolverAR.java
+++ b/SER/ser-result-detector/src/main/java/verifier/SERSolverAR.java
@@ -1425,9 +1425,15 @@ class SERSolverAR<KeyType, ValueType> {
         // other relations participating in the JOIN.
         for (var entry : scopedEntries) {
             var frontier = frontiersByKey.get(entry.key);
-            if (frontier == null || frontier.fixedWrite != null) {
-                // A query-before self-write frontier is internal and follows the
-                // existing row-local rule: WW already orders later overwrites.
+            if (frontier == null) {
+                continue;
+            }
+            if (frontier.fixedWrite != null
+                    && frontier.fixedWrite.getTxn().equals(frontier.reader)) {
+                // Only a query-before self-write frontier is internal.  An
+                // external recorded source is also stored as fixedWrite, but it
+                // is the PR_WR anchor from which later same-key writes must be
+                // checked for contextual PR_RW dependencies.
                 continue;
             }
             var externalStarted = startExternalKeyEncoding(
